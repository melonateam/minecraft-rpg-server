import type { DialoguePage } from '../../domain/project';
import type { ServerPageSettings } from '../../domain/serverSettings';
type ExtendedPage = DialoguePage & { server?: ServerPageSettings };
export function ServerPreviewBadges({ page }: { page: DialoguePage }) { const server = (page as ExtendedPage).server; if (!server) return null; const effects = server.effects; const effectCount = [effects.giveItems, effects.takeItems, effects.variablesSet, effects.variablesDelete, effects.chatInputVariable, effects.sounds, effects.message, effects.returnTarget, effects.serverCommand].filter(Boolean).length; return <div className="mb-4 flex flex-wrap gap-2 text-[10px] uppercase tracking-wider text-[#aab2c1]"><span className="rounded-md border border-white/10 bg-black/25 px-2 py-1">Flow · {server.flow.conditionalTargetPageId ? server.flow.conditionalTiming : 'normal'}</span>{server.flow.ending && <span className="rounded-md border border-amber-300/20 bg-amber-300/10 px-2 py-1 text-amber-200">Terminal</span>}{server.displayCondition.mode !== 'none' && <span className="rounded-md border border-sky-300/20 bg-sky-300/10 px-2 py-1 text-sky-200">Condition · {server.displayCondition.mode}</span>}{effectCount > 0 && <span className="rounded-md border border-emerald-300/20 bg-emerald-300/10 px-2 py-1 text-emerald-200">Effects · {effectCount}</span>}{server.operationOnly && <span className="rounded-md border border-fuchsia-300/20 bg-fuchsia-300/10 px-2 py-1 text-fuchsia-200">Operation only</span>}</div>; }
